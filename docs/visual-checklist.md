# 시각 검증 체크리스트

자동화 E2E로 잡기 어렵거나 WebView 특성상 실제 VS Code에서만 확인 가능한 항목.
main squash merge 전 사용자가 직접 체크한다.

## 라이브 편집 (CM6)

- [ ] 스크롤 모드 최초 진입 시 하단에 "실시간 편집 활성화" 토스트 표시
- [ ] 토스트 × 버튼으로 즉시 닫힘
- [ ] 스크롤 → 종이책 → 스크롤 재진입 시 토스트 재표시 안 됨
- [ ] CM6 에디터 마운트 직후(클릭 없이) `**굵게**` `*이탤릭*` `` `코드` `` 마커가 숨겨져 렌더링됨
- [ ] 커서가 위치한 줄에서는 `**` 마커가 노출됨 (live preview 정상)
- [ ] 종이책 → 스크롤 전환 시 책 HTML 잔류 없이 CM6만 표시됨 (이중 콘텐츠 없음)
- [ ] 텍스트 수정 후 300ms 뒤 파일 자동 저장 (VS Code 에디터 내용 변경 확인)
- [ ] 외부 파일 수정 시 CM6 비포커스 상태면 내용 자동 반영

## 라벨 칩

- [ ] `` `[중요]` `` 작성 시 색상 칩으로 렌더링됨
- [ ] 설정 패널 "라벨 색" 섹션에 커스텀 라벨 목록 표시됨
- [ ] 라벨 칩 클릭 → 색조 슬라이더로 색상 변경 가능
- [ ] 글꼴·줄 간격 변경 후에도 라벨 색 유지됨 (reflow 후 applyLabelStyle 호출 확인)

## 기존 기능 회귀

- [ ] 종이책 모드 페이지 넘김 정상
- [ ] 슬라이드/큐브 모드 전환 정상
- [ ] 체크박스 클릭 → 파일 토글 저장 정상
- [ ] 검색 기능 (스크롤 모드에서 CM6 진입 시 검색 비활성화, 다른 모드에서 정상)
- [ ] mermaid 다이어그램 렌더링 정상
- [ ] 목차(TOC) 정상
